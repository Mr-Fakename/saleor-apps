query GetOrderById($id: ID!) {
  order(id: $id) {
    id
    number
    created
    status
    user {
      id
      email
      firstName
      lastName
    }
    lines {
      id
      productName
      quantity
      variant {
        id
        product {
          id
        }
      }
    }
  }
}
